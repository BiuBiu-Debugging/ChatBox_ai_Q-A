This project is about an AI chatbox that answers academic questions by vectorizing the question in the database and the user's question, finding the sentence whose vectors are the closest, and answering the question. The LOSS parameter between the two vectors I use is 0.8

install libary:

pip install pandas scikit-learn joblib nltk numpy

run files in order clear_train.ipynb --> Interface_1.py
